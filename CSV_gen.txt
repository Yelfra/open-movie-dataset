psql -c "\copy (SELECT title, year, director.name AS director_name, director.surname AS director_surname, duration, rating_imdb, genre.name AS genre, country, oscarnom AS oscar_nominations, oscars AS oscars_won, actor.name AS actor_name, actor.surname AS actor_surname FROM movie JOIN starring USING(movieID) JOIN actor USING(actorID) JOIN director USING(directorID) JOIN ofGenre USING(movieID) JOIN genre USING(genreID)) to 'D:\movies.csv' (FORMAT CSV, HEADER TRUE, DELIMITER ',', ENCODING 'UTF8')" -U postgres -d postgres